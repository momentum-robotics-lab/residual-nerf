#!/bin/bash
#echo commands to stdout
set -x
# create sigularity container
source /etc/profile.d/modules.sh
SIF=/ocean/projects/cis230066p/bduister/torch.sif
S_EXEC="singularity exec -B /ocean:/ocean --nv ${SIF}"
# implement the job in the container

YOUR_SCRIPT=/ocean/projects/cis230066p/bduister/res-nerf-torch/scripts/train_all.sh


${S_EXEC} 'bash' ${YOUR_SCRIPT}

#END
